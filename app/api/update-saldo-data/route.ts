import { NextResponse } from "next/server";
import { backupDashboardState, readDashboardState, writeDashboardState } from "@/lib/dashboard-state";
const key = "update-saldo-data", fileName = "update-saldo-data.json", headers = { "Cache-Control": "no-store" };
const emptyPayload = { financeData: { accounts: [], deviceStatus: [], lastUpdated: null as string | null }, updatedAt: null as string | null };
export const dynamic = "force-dynamic";
export async function GET() { try { return NextResponse.json(await readDashboardState(key, emptyPayload), { headers }); } catch (error) { console.error("[update-saldo-data] Supabase read failed", error); return NextResponse.json({ error: "Gagal membaca data saldo dari Supabase." }, { status: 500, headers }); } }
export async function POST(request: Request) { if (!process.env.DASHBOARD_EDIT_PASSWORD || request.headers.get("x-dashboard-password") !== process.env.DASHBOARD_EDIT_PASSWORD) return NextResponse.json({ ok: false, error: "Invalid password" }, { status: 401 }); const input = await request.json().catch(() => ({})); const now = new Date().toISOString(); const finance = input.financeData ?? { accounts: Array.isArray(input.records) ? input.records : [], deviceStatus: [] }; const payload = { financeData: { accounts: Array.isArray(finance.accounts) ? finance.accounts : [], deviceStatus: Array.isArray(finance.deviceStatus) ? finance.deviceStatus : [], lastUpdated: now }, updatedAt: now }; try { payload.updatedAt = await writeDashboardState(key, payload); payload.financeData.lastUpdated = payload.updatedAt; const blob = await backupDashboardState(fileName, payload); return NextResponse.json({ ok: true, updatedAt: payload.updatedAt, url: blob?.url }); } catch (error) { console.error("[update-saldo-data] Supabase write failed", error); return NextResponse.json({ ok: false, error: "Gagal menyimpan data saldo ke Supabase." }, { status: 500 }); } }
