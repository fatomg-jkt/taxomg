import { NextResponse } from "next/server";
import { backupDashboardState, readDashboardState, writeDashboardState } from "@/lib/dashboard-state";
const key = "control-omzet-data", fileName = "control-omzet-data.json", headers = { "Cache-Control": "no-store" };
const emptyPayload = { controlOmzetData: [], updatedAt: null as string | null };
export const dynamic = "force-dynamic";
export async function GET() { try { return NextResponse.json(await readDashboardState(key, emptyPayload), { headers }); } catch (error) { console.error("[control-omzet-data] Supabase read failed", error); return NextResponse.json({ error: "Gagal membaca data control omzet dari Supabase." }, { status: 500, headers }); } }
export async function POST(request: Request) { if (!process.env.DASHBOARD_EDIT_PASSWORD || request.headers.get("x-dashboard-password") !== process.env.DASHBOARD_EDIT_PASSWORD) return NextResponse.json({ ok: false, error: "Invalid password" }, { status: 401 }); const input = await request.json().catch(() => ({})); const payload = { controlOmzetData: Array.isArray(input.controlOmzetData) ? input.controlOmzetData : [], updatedAt: new Date().toISOString() }; try { payload.updatedAt = await writeDashboardState(key, payload); const blob = await backupDashboardState(fileName, payload); return NextResponse.json({ ok: true, updatedAt: payload.updatedAt, url: blob?.url }); } catch (error) { console.error("[control-omzet-data] Supabase write failed", error); return NextResponse.json({ ok: false, error: "Gagal menyimpan data control omzet ke Supabase." }, { status: 500 }); } }
